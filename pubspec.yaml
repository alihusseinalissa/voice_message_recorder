name: voice_message_recorder
description: "A new Flutter project."
version: 0.0.1
homepage: https://github.com/Moeed366/

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.1.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  cupertino_icons: ^1.0.6
  # uuid: ^3.0.7
  permission_handler: ^11.0.0
  animated_text_kit: ^4.2.2
  path_provider: ^2.1.1
  provider: ^6.1.1
  record: ^4.4.4
  audioplayers: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.example.voice_message_recorder
        pluginClass: VoiceMessageRecorderPlugin
      ios:
        pluginClass: VoiceMessageRecorderPlugin
      linux:
        pluginClass: VoiceMessageRecorderPlugin
      macos:
        pluginClass: VoiceMessageRecorderPlugin
      windows:
        pluginClass: VoiceMessageRecorderPluginCApi
      web:
        pluginClass: VoiceMessageRecorderWeb
        fileName: voice_message_recorder_web.dart

  assets:
    - assets/

